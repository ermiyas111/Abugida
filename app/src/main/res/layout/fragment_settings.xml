<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp">

        <TextView
            android:id="@+id/keyboard_settings_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_title"
            android:textSize="20sp"
            android:textStyle="bold"
            android:paddingBottom="12dp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_sound"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sound on keypress"
            android:paddingVertical="8dp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_suggestions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Show suggestions"
            android:paddingVertical="8dp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_key_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Show key preview"
            android:paddingVertical="8dp" />

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/switch_auto_pick"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Auto-pick suggestion on space"
            android:paddingVertical="8dp" />
    </LinearLayout>
</ScrollView>
